---
title: Cursor
---

# Cursor

:::info Coming soon
Full guide in progress. Use `engress launch cursor` for tunnel + configure steps.
:::

```bash
engress launch cursor --port 11434
```

In Cursor **Settings → Models**, set the OpenAI-compatible base URL to your Engress tunnel. Use production ACME on the edge before testing — Cursor requires valid HTTPS chains.